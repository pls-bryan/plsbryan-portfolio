{
  "name": "figma-portfolio-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "postinstall": "git lfs install && git lfs fetch --all && git lfs checkout"
  }
  },
  "dependencies": {
    "astro": "^5.12.8"
  }
}